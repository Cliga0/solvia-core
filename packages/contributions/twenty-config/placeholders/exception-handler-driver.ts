/**
 * Placeholder for Twenty's ExceptionHandlerDriver enum.
 * TODO: Replace with Solvia's own exception handler driver enum.
 */
export enum ExceptionHandlerDriver {
  CONSOLE = 'CONSOLE',
  SENTRY = 'SENTRY',
}
