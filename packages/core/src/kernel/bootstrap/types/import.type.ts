import type { ModuleType } from "./module.type";

/* =============================================================================
 * Import Type
 * =============================================================================
 *
 * A runtime import is simply a module reference participating in application
 * composition.
 *
 * ImportType is intentionally an alias of ModuleType.
 *
 * =============================================================================
 */

export type ImportType = ModuleType;
